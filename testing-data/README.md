Testing files provided for project validation live in this folder so they remain separate from the app's base demo database.

- `src/data/imports/` contains the CSV files used by the bundled in-app testing dataset.
- `testing-data/` preserves the original handoff files, including the Excel workbook.
